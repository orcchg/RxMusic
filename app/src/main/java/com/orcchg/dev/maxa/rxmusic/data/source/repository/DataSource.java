package com.orcchg.dev.maxa.rxmusic.data.source.repository;

/**
 * Common interface for the source of data. Both Cloud (remote) and Database (local) sources
 * must implement this interface.
 */
public interface DataSource {
}
